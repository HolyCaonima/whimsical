# 工作约定

## 1. 始终把架构设计放在心上

落地一个功能时如果姿势别扭、需要绕路、要靠手工同步两处状态才能对上，**这是框架的问题，不是这次实现的问题**。停下来，先判断是框架哪里不对，给出框架改进方案并询问开发者，不要用局部妥协把问题埋进去。

判断信号（出现任意一条就该停）：

- 需要在两处维护同一份真相，靠约定或注释保证它们不失配。
- 需要遍历全量数据去"发现"变化，而不是变化发生时就被告知。
- 加一个正常的东西（生成一个物体、加一个 pass）会牵动不相关的全局重建。
- 需要一张映射表，把本该天然对齐的两组索引对上。
- 同一个决定散落在多个调用点，忘记其中一个就会静默出错。

**好的框架，很多好特性是自然长出来的，不需要手工编程。** 本工程的实际例子：`RenderScene` 引入稳定槽位之后，GPU instance 下标、TLAS instance 下标和 `gl_InstanceIndex` 自动就是同一个数，映射表不需要存在；proxy 按变化频率拆成 transform / attributes 两半之后，这个划分正好落在 `GpuInstance` 已有的内存布局上，"只写前 128 字节的变换快速路径"是白捡的，一行 shader 都没改。在此之前想做增量更新，只能逐帧比对两个数组——那种别扭恰恰是框架在报警。

反过来说：如果一个优化必须靠手工维护缓存、手工标脏、手工保证顺序才成立，先怀疑抽象切错了地方。

## 2. 不要写过度保护的代码

不变量应该由结构保证，而不是由散落各处的检查保证。

- 边界验证一次就够（外部输入、脚本调用、文件读取），越过边界之后信任自己的数据。
- 不要为类型系统或已有不变量已经排除的情况加检查。
- 不要吞异常、不要静默兜底、不要"出错就用默认值继续跑"。带着错误状态跑下去比当场崩掉难查一个数量级。
- 不要为"理论上可能但实际不会发生"的情况写分支——它永远不会被执行，也永远不会被测到，只会误导下一个读代码的人。
- 需要保护的地方，保护要写在**唯一的收口处**，不是每个调用点各写一遍。

契约写在类型和结构里；确实无法由结构表达的约束，才用断言明确说出来并让它响。
